<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shoe Store</title>
</head>
<style>
    :root {
        --my-color: #7a7a7a
    }

    .card {
        border-radius: 5px;
        padding: 10px;
        margin: 10px;
        width: 200px;
        background-color: #ededed;
        display: block;
    }

    .shoe-image {
        width: 100%;
        height: 55%;
    }

    .shoe-name{
        font-weight: bold;
        text-align: center;
        padding-bottom: 0.5rem;
    }

    .shoe-price{
        padding-bottom: 0.5rem;
    }

    button {
        margin: 8px 9px 5px 15px;
        background-color: var(--my-color);
        border-radius: 5px;
    }

    button:hover{
        background-color: #9e9e9e;
    }

    .place-bid-button{
        margin: 0.5rem 0rem 0rem 4rem
    }

    .delete-button{
        background-color: red;
        margin: 0rem 0rem 0rem 9rem;
    }

    .bidder-input{
        width: 5rem;
    }

    .place-bid{
        padding-bottom: 0.5rem;
    }

    .shoe-container {
        display: grid;
        grid-template-areas: '. . . . .';
        padding-left: 15px;
    }

    .search-bar{
        margin: 1rem 1rem 1rem 2rem;
    }

    .search-button{
        background-color: white;
    }

    header {
        background-color: #ededed;
        padding: 5px 0;
        margin: 0;
        display: grid;
        grid-template-areas: 'title . links';
        border-bottom: outset;
        border-color: #5f5f5f;
    }

    .links {
        grid-area: links;
        text-align-last: end;
        padding-right: 20px;
        font-size: 1.2rem;
    }

    header h1 {
        color: black;
        font-size: 3rem;
        margin: 10px 10px;
        text-shadow: 2px 2px 3px gray;
    }

    nav ul {
        list-style-type: none;
        padding: 10px 0;
    }

    nav li {
        display: inline;
    }

    nav li a {
        color: black;
        text-decoration: none;
        padding: 10px 20px;
    }

    nav li a:hover {
        color: white;
        background-color: var(--my-color);
    }

    .toolbar-title {
        align-content: center;
        min-height: 48px;
        grid-area: title;
    }

    body {
        background-color: var(--my-color);
        align-items: center;
        margin: 0;
        padding: 0;
    }

    footer {
        width: 100%;
        height: 6rem;
        background-color: #ededed;
        position: relative;
        bottom: 0;
        border-top: outset;
        border-color: #5f5f5f;
    }

    .footer-note {
        text-align: center;
    }
</style>

<body>
    <header>
        <h1 class="toolbar-title">Shoe Store</h1>
        <nav class="links">
            <ul>
                <li class="home"><a href="home">Home</a></li>
                <li class="about"><a href="about.html">About</a></li>
                <li class="cart"><a href="accountInfo.html">Account</a></li>
                <li class="sellShoe"><a href="sellShoe">Sell Shoe</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <form class="search-bar" action="/home" method="GET">
            <input type="text" name="query" placeholder="Search For Shoe">
            <button class="search-button" type="submit">Search</button>
        </form>
        <div class="shoe-container">
            <% shoes.forEach((shoe)=>{ %>
                <div class="card">
                    <form action="/deleteShoe" method="post">
                        <input type="hidden" name="id" value="<%= shoe.ID %>">
                        <button class="delete-button">Delete</button>
                    </form>
                    <img class="shoe-image" src="<%= shoe.image %>" alt="<%= shoe.NAME %>">
                    <div class="shoe-name">
                        <%= shoe.NAME %>
                    </div>
                    <div class="shoe-price">Price: $<%= shoe.price %>
                    </div>
                    <form action="/updateBid" method="post">
                        <input type="hidden" name="id" value="<%= shoe.ID %>">
                        <div class="place-bid">
                            <label for="bid">Place Bid:</label>
                            <input type="number" id="bid" name="bid" step="0.01" min="<%= shoe.bid %>" max="<%= shoe.price %>" value="<%=shoe.bid%>">
                        </div>
                        <div>
                            <label for="buyer">Bidder Name:</label>
                            <input class="bidder-input" type="text" id="buyer" name="buyer" placeholder="John Doe">
                            <button value="submit">Place Bid</button>
                            <button>Buy Now</button>
                        </div>
                    </form>
                </div>
                <% }) %>
        </div>
    </main>
    <footer>
        <p class="footer-note"><strong>Thanks for choosing Cole's Shoe Store</strong></p>
    </footer>
</body>

</html>